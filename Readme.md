Viajem normal de componentes:
Index-> ScannerScreen aqui temos os Action buttons que qunado clicamos temos o onTakePhoto(Action Button) -> processImage(useImageProcessing) -> processReceipt(receiptService)
